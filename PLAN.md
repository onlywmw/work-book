# PLAN — work-book 工作台施工计划

> 状态：**待评审**（2026-08-11 v1，依赖 REQUIREMENTS.md 拍板；开放问题 Q1-Q6 未决前不施工）
> 验收纪律沿用 MOV 验收铁律：不信自报信实物、每里程碑独立验收、UI 五态必覆盖。

---

## 一、分期（三期，每期独立可验收）

### M1：只读驾驶舱（核心闭环）
**出口**：打开应用 → 四区视图渲染真数据 → 点行打开工单。

| 子项 | 内容 | 验收点 |
|---|---|---|
| M1.1 | 数据层适配器：TASKBOARD.md 表格解析（含老格式兼容）+ ACCEPTANCE_LOG 挂账提取 + git 分支账龄 | 解析脚本可独立 node 直跑；对 MOV-APP 实测输出 40+ 滞留项、账龄正确 |
| M1.2 | Electron 骨架：main/preload/index.html 单文件 UI，contextIsolation | 启动 < 2s；无 nodeIntegration |
| M1.3 | 四区渲染：等我拍板 / 滞留区（账龄降序+红字阈值）/ 今日战线 / 挂账台账 | 五态覆盖（loading/empty/error/disabled/submitting）；空仓库显示诚实空态 |
| M1.4 | 交互：点行打开工单文档（shell.openPath）+ 手动刷新 + 底部状态栏 | 文档不存在 → 诚实错误态 |

### M2：常驻 + 主动通知（"不问就知道"的核心）
**出口**：窗口关了也在托盘；账龄跨阈值弹 Windows 通知。

| 子项 | 内容 | 验收点 |
|---|---|---|
| M2.1 | 托盘常驻 + 关窗最小化到托盘 + 开机自启（可关） | 重启后自启；托盘菜单：打开/刷新/退出 |
| M2.2 | 定时刷新（默认 30min）+ 窗口聚焦时刷新 | 数据时效有界；不重复弹通知 |
| M2.3 | 通知引擎：新增滞留/账龄跨阈值/待验收超 N 天 → toast；每事项只通知一次（状态落盘 userData） | 变异验证：同事项二次刷新不重弹 |
| M2.4 | 设置持久化：刷新间隔/阈值/自启开关（userData/settings.json） | 改设置重启仍生效 |

### M3：打磨与扩展（按需，不承诺工期）
- 多仓库适配器（Q1 若拍板多仓库才做）
- 通知点击跳转定位任务
- 历史快照对比（昨天 vs 今天滞留变化）

---

## 二、技术决策（待 Q 系列拍板后定稿）

| 项 | 决策 | 理由 |
|---|---|---|
| 框架 | Electron + 单文件 HTML（无构建工具） | 用户既定技术栈；技能库已有成熟模式 |
| 数据层 | Node.js 解析脚本，可独立 CLI 直跑 | 先有数据再有 UI；数据层可被 cron 复用 |
| 进程模型 | contextIsolation + preload 桥 | 安全基线 |
| 状态存储 | userData/settings.json + notified.json | 不进仓库，不污染数据源 |
| 参考资产 | 昨天 mov-cockpit 原型的数据层与界面骨架（Q5 待拍板） | 实测跑通，避免重复造 |

---

## 三、改动边界与纪律

1. **work-book 仓库只进本项目代码**；MOV-APP 仓库零改动（只读消费）
2. 每里程碑独立 commit + 独立验收，编译/运行不过不报 hash
3. UI 五态（loading/empty/error/disabled/submitting）每个交互必须覆盖
4. 纯文字 UI 审美红线：无图标/无 emoji/无渐变，字重字号留白分层级
5. 数据层先于 UI：解析脚本单独验收通过，再做界面

---

## 四、验收方式

- M1：实测输出对照（MOV-APP 真实任务板 → 四区数据逐项核对）+ 五态截图
- M2：通知行为实测（制造跨阈值场景 → toast 弹出 → 二次不重弹）+ 自启实测
- 每期验收人：用户本人；验收记录落 work-book 仓库 CHANGELOG.md

---

## 五、明确不做

同 REQUIREMENTS.md §七。另加施工期纪律：
- 需求未拍板不写代码
- 不引入构建工具链（webpack/vite）——单文件直跑
- 不做跨平台适配（只 Windows）
