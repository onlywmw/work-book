# REQUIREMENTS — work-book 工作台

> 状态：**需求评审中**（2026-08-11 v1，待拍板后才进入施工）
> 一句话定义：**常驻桌面的只读驾驶舱——任务卡在哪里，不问就知道。**

---

## 一、背景与痛点

用户原话（2026-08-11）："我发现好多任务都中断了，不问都不知道，我现在想做一个适合我的工作台。"

**实测证据**（2026-08-11 从 MOV-APP TASKBOARD.md 解析）：

| 事实 | 数据 |
|---|---|
| 板上滞留标记总数（⏸️/待验/挂账/待认领） | 40 处 |
| 工单 2.10 / 2.11「待真机复验」 | 各挂 8 天无人推进 |
| 工单 2.6「待真机复验必迁三项」 | 挂 9 天 |
| OCR R-SWA | 08-10 拍板实现、派单在板，无人认领 |
| feat/gig-g0 分支 | 晾着未合入，G0 relay 三件"在途"无下文 |
| 验收挂账 | 11 项散在各已验收工单里，靠"下次再说" |

**根因**：任务板是**只写**的。任务登记进去就沉底，没有机制主动浮出"卡了几天、卡在谁身上、下一步是什么"。查验员只在程序员报 hash 时醒来；用户不问，滞留就永远安静。

---

## 二、产品定义

| 项 | 定 |
|---|---|
| 形态 | Electron 桌面应用（2026-08-11 用户拍板：正式产品形态，可常驻可通知） |
| 用户 | wmw 单人，纯本地 |
| 定位 | 派单/验收体系的**只读镜像**——不是新任务系统，不取代 TASKBOARD.md |
| 核心承诺 | 不问就知道：系统主动浮出卡点，而不是靠人想起来去问 |
| 仓库 | github.com/onlywmw/work-book |

---

## 三、核心场景

1. **早上开机**：一眼看到今天的优先级——等我出手的事、卡最久的滞留、施工中的线
2. **工作中**：某任务账龄跨过阈值 → Windows 通知主动弹出（不依赖我打开窗口）
3. **追查**：点任意任务行 → 直接打开对应工单文档（TASKS_*.md）
4. **验收节奏**：哪条线已交付待验收、等了几天，一屏可见，杜绝"交付了没人验"

---

## 四、功能需求

| # | 需求 | 说明 |
|---|---|---|
| FR-1 | 数据聚合零手工 | 从仓库自动解析：TASKBOARD.md（队列表）+ ACCEPTANCE_LOG.md（挂账）+ git（分支账龄/最近提交）。**人不维护任何数据** |
| FR-2 | 四区视图 | ① 等我拍板/操作（置顶）② 滞留区（按账龄降序）③ 今日战线（施工中）④ 挂账台账 |
| FR-3 | 账龄计算 | 每任务显示卡住天数；超阈值标红（阈值可配，默认 3 天） |
| FR-4 | 主动通知 | 触发条件：新增滞留 / 账龄跨阈值 / 待验收超 N 天。Windows toast，**同一事项只通知一次**（防骚扰） |
| FR-5 | 常驻 | 托盘图标、开机自启（可关）、定时自动刷新（默认 30 分钟 + 窗口聚焦时刷新） |
| FR-6 | 跳转 | 点任务行打开工单文档（系统默认编辑器） |
| FR-7 | 手动刷新 | 一键重读仓库 |
| FR-8 | 多仓库扩展（预留） | 数据源做成适配器模型，首个适配器 = MOV-APP TASKBOARD 格式；后续加仓库只加配置 |

## 五、UI 约束（审美红线）

- 纯文字，无图标，无 emoji，无渐变，无玻璃拟态
- 白底，靠字重 / 字号 / 留白分层级
- 数字用等宽字体，正文细体
- 紧凑列表（border-bottom 分隔），不用卡片网格
- 交互五态必须覆盖：loading / empty / error / disabled / submitting

## 六、非功能需求

| # | 需求 |
|---|---|
| NFR-1 | 启动 < 2 秒（缓存先行，后台重扫） |
| NFR-2 | **只读纪律**：永不写任务板、永不改仓库任何文件；只读 + 打开文档 |
| NFR-3 | fail-safe：仓库不存在 / git 不可用 → 显示诚实错误态，不显示假数据 |
| NFR-4 | 零网络依赖（纯本地仓库解析，不连任何服务） |

## 七、明确不做（v1）

1. 不做任务管理（不支持手动建/改/删任务——那是 TASKBOARD.md 的职责）
2. 不写回仓库（只读镜像，任何"标记已处理"的操作都不做）
3. 不做云端 / 多用户 / 账号体系
4. 不做 AI 分析（v1 纯规则解析；LLM 摘要是未来项，不进 v1）
5. 不做 MOV app 内页面（形态已拍板 Electron 独立应用）
6. 不替代验收流程（验收仍在会话里做，这里只看状态）

## 八、开放问题（待拍板）

| # | 问题 | 查验员建议 |
|---|---|---|
| Q1 | 追踪范围：v1 只 MOV-APP，还是上来就多仓库（Yuanzi / Dify）？ | 只 MOV-APP 起步，适配器留扩展口 |
| Q2 | 通知触发阈值：账龄几天弹通知？（界面标红默认 3 天） | 通知比标红严：5 天，且每事项只弹一次 |
| Q3 | "等我操作"判定：关键词规则（待真机/待认领/待用户/待配合）够不够，还是要手动标记？ | v1 关键词规则，够用且零维护 |
| Q4 | 开机自启 v1 就做，还是先手动启动？ | v1 做，托盘常驻是"常驻通知"的前提 |
| Q5 | 昨天的 mov-cockpit 原型（C:\Users\Administrator\mov-cockpit，数据层已实测跑通）如何处置？ | 吸收其解析层与界面骨架作参考，正式代码进 work-book 仓库重写落档 |
| Q6 | 项目名/产品名：就叫 work-book，还是另起显示名？ | 仓库名 work-book，窗口标题可定「工作台」 |
